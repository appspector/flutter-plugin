name: flutter_appspector_example
description: A sample app which demonstrates AppSpector initialization and features.
publish_to: none

environment:
  sdk: ">=2.2.1-dev.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  appspector:
    path: ../
  http: ^0.12.0+1
  path_provider: '>=0.4.1'
  sqflite: any

flutter:
  uses-material-design: true
  assets:
    - assets/
